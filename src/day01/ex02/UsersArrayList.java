package day01.ex02;

public class UsersArrayList implements UsersList {
    // array to store user data
    // default array size 10
    // if full, increases by half
    // add user method put USER in the first empty cell
    // if try get user with non-existent id - throw unchecked except UserNotFoundException
    // ArrayList <T> - same structure
}
